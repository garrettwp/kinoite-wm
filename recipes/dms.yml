# Install DankMaterialShell
modules:
  - type: dnf
    repos:
      copr:
        - avengemedia/dms-git
    install:
      install-weak-deps: true
      packages:
        - cava
        - qt6ct-kde
        - qt6-qtmultimedia
        - danksearch
        - cliphist
        - matugen
        - dgop
        - quickshell-git
        - dms

