# Install Sway Notification Manager
modules:
  - type: dnf
    repos:
      # Sway Notification Center COPR Repo
      copr:
        - erikreider/SwayNotificationCenter
    install:
      install-weak-deps: false
      packages:
        - SwayNotificationCenter
