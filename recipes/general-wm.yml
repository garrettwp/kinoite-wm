# Install general Window Manager packages
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - waybar
        - fuzzel
        - pavucontrol
        - brightnessctl
        - playerctl
        - network-manager-applet
        - blueman
        - wl-clipboard
