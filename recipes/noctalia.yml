modules:
  - type: dnf
    repos:
      files:
        - terra.repo

    install:
      skip-unavailable: true
      packages:
        - repo: terra
          packages:
            - noctalia-shell
        - dejavu-sans-fonts
        - jemalloc 
